# Novae repository structure

This document is the maintained map of the repository. Read it before broad searches and update it whenever files or responsibilities move.

## Runtime and entry points

- `package.json` / `bun.lock` — Bun 1.4 package management with a pinned install graph; Node 24 remains the Next.js, build-script, and Worker runtime alongside Next.js 16.3, React 19.2, TypeScript 7, Tailwind CSS 4, Radix/shadcn primitives, Motion, Vditor 4 WYSIWYG Markdown editing, Serwist, Vitest, and Playwright.
- `src/app/` — Next App Router. Route `page.tsx` files assemble views and forward events; they do not import `services/` directly.
- `src/app/layout.tsx` — root metadata, stable immersive iOS viewport/status-bar configuration, Inter/Roboto Mono plus HarmonyOS Sans TC split-font loading, global providers, global CSS, and nonce-bound critical Managed Turnstile preconnect/loading before hydration.
- `src/app/globals.css` — website-aligned cool blue light/dark surfaces, semantic brand actions and custom accent themes, compact typography and radius scale, restrained elevation, safe-area and stable PWA viewport sizing, keyboard-aware navigation, input-modality focus handling, and the shared discussion composer dock.
- `src/assets/fonts/harmonyos-sans-tc/` — generated, project-character-scoped HarmonyOS Sans TC Regular, Medium, Semibold, and Bold shards plus CSS; refreshed by `scripts/generate-harmonyos-subset.mjs`.
- `src/styles/motion.css` — every motion recipe in the product, written entirely in terms of a ladder it does not own. Covers route push/pop and replacement, the press vocabulary, menus, dialogs and the mobile bottom sheet, tabs, switches, toasts, skeleton-to-content handoff, success confirmation, and reactions. The pacing is iOS: a touch is acknowledged in a tenth of a second, a control settles in a quarter, anything genuinely new — a route, a panel, a toast, content replacing its own skeleton — takes long enough to be read as arriving, and a layer presented over the current place takes the longest. What it deliberately leaves alone matters as much: furniture does not fade in, lists do not stagger, and anything already on screen when a route arrives is carried by the route transition instead of animating again underneath it. Reduced-motion fallbacks are centralized here.
- `src/styles/vditor-editor.css` — scoped Vditor toolbar, WYSIWYG content, focus, light/dark token, mobile keyboard, reduced-motion, and loading-surface overrides aligned with Novae's design system; wide tables scroll inside the bounded editor and table controls scroll without being clipped.
- `src/app/sw.ts` — Serwist service worker; `next.config.mjs` compiles and registers it at `public/sw.js`, while WOFF2 font shards stay outside install-time precaching and load normally through their unicode ranges.
- `src/proxy.ts` — per-request nonce CSP for Next hydration, strict production script execution, Firebase App Check/reCAPTCHA, Cloudflare Turnstile, and local-emulator development connections.
- `next.config.mjs` — public environment injection, static security headers, image hosts, and Serwist integration.
- `next-env.d.ts` — ignored framework-generated type references; Next.js regenerates the file for the active development or isolated verification output directory.
- `components.json` — shadcn/ui aliases and Tailwind 4 configuration.
- `public/` — brand identity assets (`logo.svg`, `logo.png`, `favicon.ico`, `apple-touch-icon-180x180.png`, `maskable-icon-512x512.png`, `pwa-*.png`), constellation login artwork, and compiled service worker runtime.
- `vercel.json` — Vercel Next.js framework configuration with an explicit frozen Bun install command for CLI and hosted builds.

## App Router

- `src/app/login/` — focused Google sign-in presentation with the website's blue brand statement and desktop constellation image; the mobile authentication path stays compact.
- `public/novae-constellation.webp` — original website brand artwork, reused on the desktop login surface with reserved image dimensions.
- `src/app/(protected)/layout.tsx` — authenticated application guard and shared shell boundary, including the single `@sheet` parallel slot for records and the issue composer opened from protected pages. `@sheet/(.)issues/[filter]/[issueId]`, `@sheet/(.)issues/[filter]/compose/new`, `(.)facilities/[facilityId]` and `(.)announcements/[announcementId]` own the intercepted presentations. `default.tsx` and `[...dismissed]/page.tsx` clear the slot on direct arrivals and navigation away. Each direct record route's `layout.tsx` mounts `DirectDetailSheet`, retaining the same sheet around its loading boundary and closing to the owning list.
- `src/app/(protected)/setup/` — admin-only idempotent category setup with completion recovery in `use-initial-setup`; first-visit language selection happens through the shared locale gate before any route.
- `src/app/(protected)/issues/` — feature-guarded issue redirect, feed, detail routes, and the `/issues/[filter]/compose/new` composer route whose four-segment shape stays outside the intercepted record URL.
- `src/app/(protected)/facilities/` — feature-guarded facility feed, composer, and detail routes.
- `src/app/(protected)/announcements/` — announcement feed, composer, and detail routes.
- `src/app/(protected)/notifications/` — merged broadcast/admin/user notification presentation with a route-loading shell and no hover-triggered destination prefetch.
- `src/app/(protected)/settings/` — account, appearance, language, install, push, and resource settings using the full shared desktop content width.
- `src/app/(protected)/admin/` — the administration area, one responsibility per route: `/admin` is both the landing page and the merged platform overview, and `content`, `platform`, `people`, `audit`, `system` and `policies` each own one screen, one permission gate, and one save. `layout.tsx` gates the area, mounts the apply-progress panel once, and guards departure from an unsaved draft; each route carries a geometry-matched `loading.tsx`. The separate `/dashboard` route and the tabbed `/admin/management` route it replaced are gone, with no compatibility redirects.

## Presentation components

- `src/hooks/use-composer-base.ts` owns the shared upload/create transaction and synchronous submission lock; `use-entry-composer.ts` supplies domain-specific creation and navigation. `use-composer-draft.ts` and `src/lib/composer-draft.ts` own account/category-scoped, expiring tab-session text drafts; `src/components/composer-draft-notice.tsx` presents save/restore failures and confirmed text clearing. Markdown editor blur/pagehide flushes its debounced value before navigation.
- `src/services/notification-cursor.ts` preserves database timestamp precision for notification pagination; forward migrations `0048` and `0049` correct inbox cursors, expiry filtering and monotonic read watermarks. `use-notifications-page.ts` shares the paged request guard so an old response cannot overwrite a refreshed inbox.

- `src/components/motion/resize-motion.tsx` / `state-transition.tsx` / `stagger.tsx` — opt-in, height-only state-container resizing plus shared content entry/exit and dynamic-list presence animation; viewport reflow is excluded, physical card frames survive state replacement, observers clean up after disconnected nodes, and reduced motion is honored.
- `src/components/ui/feed-list.tsx` — persistent physical card slots for loading, content, error, and empty states; domain content stays keyed by entity inside the retained frame.
- `src/components/ui/detail-layout.tsx` — persistent detail content and reaction/timeline cards; record loading replaces only their contents.
- `src/components/ui/header-backdrop.tsx` — the shared paint-only background sibling for page, detail, sheet and alert headings. Its non-negative stacking layer sits below unfiltered title/control siblings; page headers use an independently measured status-bar blur for the sticky clearance above them, followed by three title-only overlapping blur bands whose 12px, 6px and 2px radii retreat only after the full measured-by-layout header height. Contained headings retain the compact single layer appropriate to their own surface.
- `src/components/ui/skeleton-rows.tsx` — shared pending rows for discussions and administrative lists. Skeleton animation phases follow the document clock.
- `src/components/ui/list.tsx` / `list-controls.tsx` — the grouped list: a card owns the gutter, `.rule-list` draws the hairline between rows, and the bleeding tint layer on `.t-row` lets a press or a selection reach the card's rounded corners while the dividers stay inset. Rows are whole-row targets; a choice is marked with a check that fades in rather than a box. A group carries no explanatory sentence: the rows say what they are. `ListRowGroup` is the one way a setting reveals further rows, so they travel through their own height in both directions instead of appearing and vanishing. Settings, notifications and administration screens are built from these instead of label-above-a-box forms.
- `src/components/ui/record-list.tsx` — the search field, card, first-read/empty/failed states and paging that a searched administrative record list wears. What a record looks like belongs to the screen that owns it: a person and a recorded action are each two lines rather than a row of columns, because as columns both became a stack of labelled fields on every screen narrower than a desktop. People are read two abreast through the `.rule-grid` recipe, so a wide card stops running off the bottom of the screen.
- Custom accent controls, provider, persistence module, and the separate `ResizableCard` implementation are removed. All cards use `Card`; selected tabs use the fixed brand accent.
- `src/components/ui/` — business-free shared primitives. Primary actions use the card surface with a defined border; accent remains for brand emphasis, selected states, and links. Secondary/cancel controls use neutral surfaces and destructive actions retain warning color. Fields, overlays, tabs, menus, badges, skeletons, page states, loading indicators, image presentation, and branding share semantic tokens. `page-state.tsx` keeps each route's main header pinned beneath the safe area and owns its height-following three-step progressive title blur, so scrolling content is softened behind the complete title/action geometry without filtering either foreground layer; the status-bar clearance is a separately measured blur layer, preserving the title geometry across iOS status-bar differences. A subpage's back control is handed to the header as its `lead` rather than placed above it, so it is pinned with the title instead of scrolling behind the veil the header draws. `card.tsx` and the shared resize observer preserve text geometry during resizing; `skeleton-reveal.tsx` overlays placeholders on the actual final field footprint.
- `src/components/ui/dialog.tsx` / `sheet.tsx` / `alert-dialog.tsx` — centred dialogs keep their own compact presentation; every bottom sheet goes through the shared `Sheet` surface, which owns the common close control, mobile page gutter, full available height, and the same drag-to-dismiss mechanics. Feature code never opts into `DialogContent`'s internal sheet presentation directly. A sheet's content stays mounted while it closes. Destructive confirmation keeps the always-centred `AlertDialog`.
- `src/components/ui/decision-sheet.tsx` — one decision, made once: the outcomes as choice rows, the sentence it is recorded with as the last row, and a single control that is the only thing that writes. `DecisionSheet` is that form arriving as a sheet, and `DecisionForm` the same form inside a sheet that also has something to report — account restriction, proposal moderation and facility conclusion are the same act and now the same surface.
- `src/components/ui/action-menu.tsx` — the few things that can be done here: a menu hung off the control on a pointer, and a sheet of whole rows on a phone. The account menu and a record's own `⋯` are the same object through it; deleting still asks the centred question behind it, because a sheet invites dismissal and that is the one thing that cannot be undone.
- `src/components/ui/choice-select.tsx` — one choice out of a few, asked in the shape the screen calls for: the menu beside the control on a pointer, and a sheet from the bottom edge with the options as ordinary choice rows on a phone. `ListPicker` is the same choice worn as a row of a list. Every picker in the product goes through it, so a sort, a category and a settings row all ask the same way.
- `src/components/ui/sheet-row.tsx` — a row that opens what it names in a sheet: the supporters of a proposal, a policy revision, the whole table list and a provider's raw reply. Nothing in the product opens downwards any more — a record pushing the rest of a list off the screen is not a place to read it, so the row carries a chevron and what it names arrives over the page and leaves the way it came. A setting that reveals the fields it governs is not this: those rows belong to the switch above them and travel through their own height with `ListRowGroup`.
- `src/components/ui/feed-card.tsx` — shared compact title/author/action geometry and grid for proposal, facility, announcement, loading, and empty cards; `FeedProgress` keeps deadline and count in one row so optional deadlines cannot shift the footer.
- `src/components/ui/status-distribution.tsx` — the share of a category held by each status: one proportional bar plus the figures beneath it, fed by counts the list snapshot carries. `status-badge.tsx` owns the status colour tokens both of them read.
- `src/components/ui/feed-toolbar.tsx` — one search/sort control pair used by live feeds and disabled route placeholders, without a redundant outer card. The field itself lives in a `popover.tsx` panel behind a search trigger at every width, so `PageHeader`'s `toolbar` slot keeps both controls beside the status tabs instead of on a line of their own; a dot on the trigger marks a query that is still applied.
- `src/components/ui/list.tsx` — the grouped list: rows that report, rows that lead somewhere, and rows that change something. A row that writes never takes the whole width as its target: `ListMutationRow` reports and opens, and `RowAction` is the single glyph beside it that acts, so a list of destructive controls cannot be set off by a stray tap.
- `src/components/ui/progress-bar.tsx` — how far along something is, as one line; shared by the background-work panel and the operations screen's running jobs.
- `src/components/ui/popover.tsx` — the shared floating panel primitive: dropdown geometry and motion for content that is a small form rather than a menu.
- `src/components/ui/image-preview.tsx` — the one way an image is shown before it is opened: a square tile the picture is fitted inside rather than cropped to, the grid those tiles align in, and the lightbox opening one leads to. Shared by attached content images and composer attachments.
- `src/components/ui/detail-card.tsx` — shared detail header and body for proposals, facilities, announcements, and their loading placeholders; badges, title, and metadata reserve the same minimum heights. `DetailBadge` is the neutral pill each of them opens with, so a proposal's category, a facility report's category, and an announcement naming itself are one shape.
- `src/components/motion/` — product-consumed animated numbers and reactions, the route surface, persistent navigation feedback, list entry/exit/reordering, and non-overlapping state replacement transitions. `navigation-feedback.tsx` answers a tap on a destination by marking the tapped control itself with `data-navigating` until the route commits, so the answer travels with the control as the page scrolls, and it starts on the click, which a touch that becomes a scroll never produces; the progress line waits out a control rung before it appears, so a route that commits immediately never draws one. `route-surface.tsx` derives navigation direction from the two pathnames and publishes it on the page that arrives, so an in-app back button, the browser's Back and Forward buttons, and the iOS edge swipe all resolve to the same push or pop. The reveal is one animation on that page, in the live document: a child arrives from the trailing edge, its parent from the leading one, and a replacement simply appears. Nothing is captured — a document snapshot rasterises both pages exactly when the browser is fetching and rendering the route that was asked for, and suspends hit testing for the length of the animation. Reaction hooks still update immediately, confirm in the background, and roll back with retry feedback on failure.
- `src/components/app-providers.tsx` — root client providers plus global input-modality ownership; keyboard activity enables focus-visible presentation, while pointer movement/down immediately returns controls to pointer presentation.
- `src/components/app-shell.tsx` / `liquid-nav.tsx` — desktop and mobile navigation. Selection follows the committed pathname; Next Link owns pending feedback and shared-layout motion moves the selection. Primary navigation links remain the small, high-value set that Next may prefetch; feed records and other bulk destinations do not. The dock is live throughout a navigation — nothing is captured, so it never leaves the hit-test tree and never has to be produced again afterwards. It animates nothing of its own: it belongs to the route, and the route change already covers it or uncovers it. On mobile the route surface owns the page gutters and vertical padding, and the floating dock appears exactly where `route-hierarchy.ts` says primary navigation belongs. Because both bars are lifted out of the document flow and are positioned from the live viewport height, the selection is measured inside the bar and moved with an explicit transform rather than by comparing two viewport rectangles taken at different moments — which is what used to send it flying in from wherever the bar had been. With no control selected it fades out where it stands. The main column is an inline-size container for header backdrops, so their horizontal extent follows the drawable app column instead of viewport units that can include a WebKit scrollbar gutter. Route headers retain their sticky progressive blur.
- `src/lib/route-hierarchy.ts` — the single description of how routes nest, including the fact that settings owns the administration area whose URLs sit outside `/settings`, and the fact that `/issues` is a doorway that forwards to a category rather than a place of its own, so crossing it carries no direction. It also names the destinations primary navigation points at, so whether a route reveals in place, and whether the floating dock belongs on it, are answered from one table rather than from a regular expression in the shell. Navigation highlighting, navigation direction, and the dock all read it; it performs no routing of its own.
- `src/lib/share.ts` — shares the current page, marking the link so the tab it is opened in knows it arrived from somebody else. Inside a messaging app's browser the marker is written into the address instead of removed, so the handoff carries it whether it goes through Novae or through the app's own menu; nothing arrives in one of those browsers except through a link somebody sent. Links shared before the marker existed are recognised by shape instead: a tab with no history of its own that opened straight onto one piece of content.
- `src/lib/avatar-cache.ts` — the avatar this browser was last given, kept for the day the worker would answer from its own copy anyway, so opening Novae again is not a rate-limited write.
- `src/lib/navigation-memory.ts` — remembers the immediately preceding in-app pathname solely to choose safe detail and administration return targets, and performs the return itself. Route rendering and browser history remain native App Router responsibilities.
- `src/components/issues/` — issue cards, the shared proposal composer (`issue-composer.tsx`, rendered as a direct page or an intercepted sheet), the detail itself (`issue-detail.tsx`, rendered both by the route and by the sheet the feed opens over itself), detail content/actions, moderation presentation, and the administrator-only local author-visibility control used when presenting a proposal.
- `src/components/facilities/` — facility cards, shared detail content with status-aware conclusion presentation, status-dialog presentation, and the category and location fields a facility report adds to the shared composer, which take the same pending state the rest of the composer does.
- `src/components/announcements/` — announcement cards and the yellow new-announcement header notice shown on primary content feeds.
- `src/components/settings/` — `account-section.tsx`, `appearance-section.tsx`, `notification-card.tsx` and `settings-links.tsx`: the whole route is one stack of grouped lists, including who is signed in, how the app looks and reads, one-way device Push enablement, platform-admin-only notification categories, whether it is installed, and signing out. The route has no `loading.tsx`: its own UI has no domain read to wait on, so a skeleton would only be a second copy of the page to keep in sync.
- `src/components/notifications/notification-skeleton.tsx` — route and local notification loading rows that share the resolved list container, padding, icon column, and text anchors.
- `src/components/admin/admin-page.tsx` / `admin-page-skeleton.tsx` / `admin-list-skeleton.tsx` — the frame every administration route wears, and the matching loading shell each route's `loading.tsx` renders. The shell carries the real back control rather than a placeholder of one, because it works while the rest of the route is still arriving. The skeleton reads no session and no query string, so a route-loading fallback stays statically prerenderable.
- `src/components/setup/` — setup step chrome and reusable category draft editors.
- `src/components/admin/` — one component set per administration route: the merged overview (`admin-overview.tsx`, `overview-metrics.tsx` — where each figure for the period opens the share of the period's activity that it counts in a sheet, out of the reading the screen already holds, instead of a separate feed underneath answering none of them — `overview-health.tsx`, `admin-activity-feed.tsx`, `activity-breakdown.tsx`), the destination index (`admin-sections.tsx` over `src/lib/admin-routes.ts`), category and feature configuration (`category-management.tsx`, `category-editors.tsx`, `category-delete-action.tsx`), retention and upload limits (`platform-settings.tsx`, `retention-groups.ts`, `platform-setting-fields.ts`), people, account access rules, and scoped responsibility (`people-console.tsx`, `user-management.tsx`, `user-details-dialog.tsx`, `account-access-rules.tsx`, `account-access-rule-fields.tsx`, `access-management.tsx`), the record log (`audit-console.tsx`, `admin-audit-log.tsx`, where a recorded action opens in full rather than being cut off inside a column), runtime policies (`policy-settings.tsx`, `policy-history.tsx`), and the failure surface (`system-console.tsx`, whose failures, capacity and providers are three views rather than one scroll, over `system-queue.tsx`, `system-capacity.tsx`, `provider-diagnostics.tsx`). `notion-rebuild-action.tsx` confirms and queues a complete background rebuild of the Notion archive. A failure is a row that names its own refusal (`failure-row.tsx`) over the one reading of jobs, deliveries and cleanup backlog that `system-failures.ts` builds, and it opens in full in `failure-detail-sheet.tsx`; one control above the list retries everything at once. A category is a row that opens its decisions in a sheet, including whether an author may delete their own proposal or facility report; category-scoped managers retain management deletion independently. Every retry in the product lives in `system-queue.tsx`; `apply-review-dialog.tsx` is the single before-and-after confirmation and `apply-progress.tsx` the single progress panel. Stateful reads and mutations stay in domain hooks.
- `src/components/composer-route-skeleton.tsx` — the composer routes while their own chunk is still arriving: the real layout and fields in their pending state, so nothing about the skeleton can drift from the form it stands in for.
- `src/components/composer-layout.tsx` / `composer-fields.tsx` — the frame every composer route is drawn in, and the title, guided Markdown editor, character counter, and media-attachment fields inside it. The frame keeps its card inside the page gutters at every width, and the fields split into an editor column and an attachment column once the screen is wide enough to hold both. Both take a pending state, so a composer route skeleton is these components stood down rather than a separate drawing of them that can drift from the real geometry.
- `src/components/markdown-editor.tsx` — client-only Vditor 4 WYSIWYG adapter with a touch-sized priority toolbar, localized overflow actions and built-in table row/column controls, pinned parser/icon CDN configuration, IME-safe length handling, mobile paste/drop image interception, accessibility wiring, and lifecycle cleanup. The editor contains its own inline size, so wide editor content cannot widen the composer card or the page.
- `src/components/composer-media-attachments.tsx` — independent image attachment picker, previews, deletion controls, and mobile-friendly attachment guidance kept outside the Markdown editor. Previews are the shared `ui/image-preview.tsx` tiles and open in its lightbox, so an attachment looks and opens the same way before and after it is published, and they fill whatever width the panel is given.
- `src/components/discussion.tsx`, `comments/comment-composer.tsx`, `comments/comment-thread.tsx` — shared server-sorted discussion card, fixed safe-area composer with vertically aligned 40px avatar/action geometry, focus-ring-free textarea presentation, reply-target preview, and collapsible reply-rail presentation. The dock publishes only its ResizeObserver-measured height; CSS owns safe-area clearance so visual viewport and keyboard changes cannot inflate page padding.
- `src/components/content-author.tsx` — shared author avatar and name row for list/detail content; unresolved profiles reserve a 24px avatar plus 64px mixed-script name skeleton and never flash a generic member label.
- `src/lib/render-markdown.ts` — the sanitizer for untrusted Markdown; inline images are forbidden so attachments render only through `DecodedImage`.
- `src/components/content-renderer.tsx` — Markdown/media presentation over `render-markdown.ts`; its optional field-level reveal affects Markdown text only and leaves media/card chrome outside the filter layer. Attached images are drawn with the shared `ui/image-preview.tsx` tiles and opened in its lightbox.
- `src/components/content-resolution-notice.tsx` / `content-resolution-notice-skeleton.tsx` — shared success/error conclusion block for closed proposals and facility reports, paired with a lightweight field-level skeleton shown only after the closed status is known; the resolved block uses status-aware copy and reduced-motion-safe state entrance animation.
- `src/components/detail-sheet.tsx` — the retained route sheet used by record details and the proposal composer. The protected shell's global interception keeps the source page and its scroll mounted; closing finishes its animation before stepping back. `DirectDetailSheet` gives direct record arrivals the same presentation with a deterministic owning-list return instead of an unrelated history entry. `useCloseRouteOverlay` and `useRouteOverlayLabel` share the close path and optional heading with controls inside the sheet.
- `src/components/detail-toolbar.tsx` — shared, geometry-stable secondary toolbar. Its back control is also where a visit that began on a shared link is asked whether Novae is already installed, because that is the moment the reader wants more than the page they were sent. Every route that primary navigation does not point at wears it, so a detail, a composer and an administration area all carry the same back control in the same place.
- `src/components/detail-actions-menu.tsx` — what else can be done to the record a detail route is showing: the overflow menu, its optional management item, and the confirmation behind its deletion, shared by proposals, facility reports and announcements.
- `src/components/protected-app.tsx`, `app-providers.tsx`, `app-update-gate.tsx`, `app-install-prompt.tsx`, `app-install/prompt-step.tsx`, `app-notification-prompt.tsx`, `app-locale-gate.tsx` — startup/session, providers, theme/i18n, transitions.dev-matched pill toast boundaries, bounded forced PWA updates with version polling and reload recovery, session-scoped proactive mobile install guidance, authenticated desktop/installed-PWA notification opt-in guidance using the current dialog system, and the local-only first-visit language gate shown until the browser has an explicitly chosen locale.
- `src/components/turnstile-provider.tsx` — Managed Cloudflare Turnstile execution for login and session verification. Signing in shows the check in place, always, because nothing may happen before it passes; restoring a session runs it silently and only raises a surface when Cloudflare asks for a person, so returning to the app no longer means answering for it. The root layout owns early script preloading and preconnecting, while tokens remain in memory and single-use at the backend boundary.
- `src/components/feature-route-guard.tsx` — shared disabled-feature guard for direct proposal and facility routes.
- `src/components/ui/route-skeleton.tsx` and route `loading.tsx` files — prefetched list and detail app-shell fallbacks that reuse the same title, permission-aware action bar, header/grid/control geometry as their resolved routes while skeletonizing only unresolved domain fields. Feed skeletons mirror each domain card's author, progress/location, status, and interaction rows; their empty states retain the same grid and card geometry while replacing data fields with the resolved message/action. Detail fallbacks do not speculate about status-dependent conclusion content, and no domain requests are started by the fallback.
- `src/components/ui/tooltip.tsx` — shared fine-pointer-only tooltip capability boundary; touch and non-hover devices keep labelled controls without opening tooltip layers.
- `src/components/ui/loading-spinner.tsx`, `action-feedback-icon.tsx`, `pending-alert-dialog-action.tsx` — shared baseline-independent loading spinner and transitions.dev-style spinner-to-check primitives for backend mutation, destructive confirmation, and update feedback.

## Hooks and stateful flows

- `src/hooks/session-store.ts` — the React-free session store: Firebase authentication, restored-session security verification, new-login-only backend profile synchronization, authoritative role/permission bootstrap, setup status, and supported-issue session state.
- `src/hooks/use-session.tsx` — the React surface over `session-store.ts`: `SessionProvider`, the `useSession` snapshot, and the permission/login/logout callbacks.
- `src/hooks/use-categories.ts` — category and platform-feature store.
- `src/hooks/use-issue-feed.ts`, `use-issue-detail.ts` — issue list/detail fetching, pagination, comments, support, and delete flows; successful deletes retain the mounted detail entity through the success hold/navigation while a shared deletion marker removes it from list snapshots, preventing a transient not-found page. Categories without comment capability short-circuit comment reads and presentation.
- `src/hooks/use-facility-feed.ts`, `use-facility-detail.ts`, `use-facility-status.ts` — facility list/detail, affected-user, delete, and moderation flows.
- `src/hooks/use-announcement-feed.ts`, `use-announcement-detail.ts`, `use-announcement-notice.ts` — announcement list/detail, likes, comments, deletion, and the account-synchronized notice that refreshes after offline or realtime recovery.
- `src/hooks/use-notifications-page.ts`, `use-notification-badge.ts` — notification aggregation, realtime subscriptions, pagination, and unread hints. The notification page renders valid targets as non-prefetched record links, so they share immediate sheet feedback with feed links; removed records remain a labelled button with an informational message.
- `src/components/optimistic-detail-navigation.tsx` — record links do not need their route prefetched to feel immediate. A click from a feed opens the shared sheet locally in the same event; the intercepted route and its data load behind that already-moving surface. When the routed detail mounts it claims the presentation and suppresses a second entrance, so the user sees one sheet fill with content rather than waiting on RSC or paying for speculative detail requests.
- `src/lib/stage-depth.ts` — nested-sheet stack and background hold ownership. A record sheet supplies the source scroll captured before its portal commits, then the stage records document height before publishing `data-stage-held`, so Radix focus/scroll locking and the fixed mobile stage cannot replace the source position before measurement. Intercepted-sheet dismissal temporarily takes manual history-scroll ownership and reapplies that position only after `popstate`, the App Router pathname commit, close autofocus, the final surface release and the dialog's body scroll unlock.
- `src/hooks/use-entry-composer.ts` — issue/facility/announcement composer workflows and upload rollback.
- `src/hooks/use-initial-setup.ts` — setup validation, persistence, polling, and retry-safe completion.
- `src/hooks/use-category-management.ts`, `use-platform-settings.ts`, `use-access-management.ts`, `use-operation-policies.ts` — category configuration, platform retention/media-limit configuration, category-scoped RBAC, and runtime policy flows. All four hold a `useDraft` and reach the backend only from its `submit`, and all four keep their last reading on screen while the fresh one arrives, so returning to a settings screen shows its values rather than a skeleton.
- `src/hooks/use-draft.ts` / `src/lib/draft-diff.ts` / `src/components/ui/save-bar.tsx` / `src/components/admin/apply-review-dialog.tsx` — the one explicit-save mechanism: an editing session over a stored value, the field-by-field difference it would write, the single control that writes it, and the confirmation that shows both the difference and the records it would disturb. The save bar and the background-work panel both arrive and leave along the edge they are pinned to, so neither appears or disappears between two frames.
- `src/hooks/share-entry-store.ts` / `use-share-entry.tsx` — whether this tab was opened from a link somebody shared, read from the URL at module load because signing in rewrites the route. A shared page shows itself and nothing else; the install conversation waits for the reader to leave it.
- `src/hooks/unsaved-changes-store.ts` / `use-unsaved-changes.tsx` — what the current screen would lose, and the guard that stops an in-app link from discarding it. The browser's own Back button is out of reach: the App Router exposes no cancellable navigation event, and faking one with a history sentinel would be worse than the gap.
- `src/lib/poll-schedule.ts` — how long to wait before looking again at work the reader just queued; it decelerates and settles instead of asking once a second forever.
- `src/hooks/use-push-token-heartbeat.ts`, `src/services/push-token-registration.ts` — authenticated-shell push-token confirmation, seven-day client throttling, shared-device token reassignment, and one-way settings-page enablement.
- `src/services/push-notifications.ts` — push permission, device token registration, and platform-admin notification preference reads/writes; split out of `notifications.ts`, which keeps the notification feed, read state, and badge.
- `src/hooks/use-remembered-state.ts` — the reading an administration panel keeps while nobody is looking at it, held in the same view memory the feeds use and scoped to the signed-in account. Every administration panel is built on it, so leaving a screen, or moving between the views of one screen, no longer throws the reading away and asks the backend the same question again; a panel reports a cold read only when there is genuinely nothing to show.
- `src/hooks/use-admin-overview.ts` — the one reading behind `/admin`, merging the platform dashboard with the administrative activity figures. It reads the dashboard through the existing short-lived cache instead of forcing past it, and changing the reporting window re-reads only that window, the first time that window is asked for.
- `src/hooks/use-admin-console.ts` / `use-system-console.ts` — user search/restriction and audit-log flows, and the combined queue/capacity surface. Users and the audit log are one paged, searched reading over two tables rather than two copies of it. Every failed background job, deletion included, is one row in one panel with one retry behind it, and that retry removes the row it belongs to rather than re-reading the whole console.
- `src/hooks/use-permission-redirect.ts` — shared client-side redirect for authenticated routes that require a specific permission.
- `src/hooks/use-push-notifications.ts`, `use-pwa-install.ts`, `use-app-install-prompt.ts`, `use-app-notification-prompt.ts` — device push preferences, explicit install actions, the standalone-aware automatic install prompt flow, and a session-scoped notification prompt for authenticated desktop or installed-PWA sessions whose browser permission remains undecided; mobile web notification enablement continues through installation guidance, while settings present each push write through the shared targeted spinner-to-check lifecycle.
- `src/hooks/use-image-attachments.ts`, `use-resolved-markdown.ts`, `use-public-profiles.ts`, `use-cold-data-reveal.ts` — reusable media, Markdown, synchronous public-profile memory hydration, and cold-read display state helpers. Resolved content replaces its loading presentation in the same render cycle.
- `src/hooks/use-content-invalidation-refresh.ts`, `use-content-realtime.ts`, `use-action-feedback.ts` — shared stale-cache refresh wiring, deployment-configurable route-scoped content realtime lifecycle, and 500 ms successful-action feedback lifecycle.

## Data, domain, and infrastructure

- `config/operations.config.json` / `scripts/generate-operations.mjs` — operational policy bounds and generated frontend/Worker contracts; quota defaults are sourced from rate-limits config.
- `cloudflare/src/backend/shared/operation-policies.ts` — the one way runtime policies are read and the one way they are reached: an entry point enters `withOperationPolicies` and everything under it asks `operationPolicy(key)`, rather than a value threaded through every call. An isolate re-reads the stored settings at most once a minute; the console that edits them reads afresh and forgets that cache when it saves.
- `cloudflare/src/backend/shared/operational-telemetry.ts` — bounded daily error aggregation without request bodies or credentials.
- `cloudflare/src/backend/shared/provider-diagnostics.ts` / `src/hooks/use-provider-diagnostics.ts` / `src/lib/provider-diagnostics-format.ts` / `src/components/admin/provider-diagnostics.tsx` / `provider-readings.tsx` — administrator-only provider diagnostics using server-side credentials, with explicit unavailable and unconfigured states. Each provider answers in its own dialect, so the reply is read into named figures and shown as the same rows the rest of administration uses; the response it arrived in stays one row away, in a sheet, for the questions the figures do not answer. The two providers that report standing figures are read as the panel opens; the log stream is a search and waits to be asked.
- `cloudflare/src/backend/actions/operations.ts` — platform-admin capacity, job, delivery, policy and policy-history management.
- `src/lib/operation-policies.ts` / `src/services/operations-console.ts` / `src/hooks/use-operation-policies.ts` / `src/components/admin/policy-settings.tsx` — runtime client policies, edited as a draft with a required reason and an audited revision history. The client holds the revision it was last given; every successful response names the revision it was produced under, so a change is noticed from traffic the page was making anyway and costs one request, and a client that is asking for nothing asks for nothing.
- `database/migrations/0022_runtime_operations.sql` — revisioned operations settings, audited changes, daily error aggregates and capacity samples.

- `src/services/` — frontend boundary for the Cloudflare Workers API, activity-aware native WebSocket realtime transport, uploads, Firebase-backed sessions, backend actions, and the session-seeded runtime push-confirmation interval. Every API operation creates one UUID sent as `X-Novae-Operation-Id`; request/response cursors use camelCase UTC ISO timestamps. Realtime closes after 30 idle minutes, suppresses reconnects until activity or foreground return, then deduplicates subscriber resync; `realtime-heartbeat.ts` pings the open connection so it is neither dropped in silence nor left waiting on a socket that has already died. Every verified Firebase session synchronizes its backend profile and reconciles `ADMIN_EMAILS` before access bootstrap; Firebase token validation warms during Turnstile execution but is consumed only after unchanged Siteverify succeeds. Every browser API request obtains an App Check token through the shared security helper, and no browser database client exists.
- `src/services/announcement-notice.ts` — the small frontend boundary for the account-scoped announcement unread probe and bounded mark-through write; announcement content reads and mutations remain in `announcements.ts`.
- `src/lib/motion-timing.ts` — the one way an animation driven from JavaScript names a duration or a curve; it hands back rungs of the generated ladder so Motion and the stylesheet stay in step.
- `src/lib/` — framework-independent request, Firebase/App Check, Google identity, in-app browser and PWA-install contracts, bounded persistent and memory caching, Markdown, image, route, formatting, pagination, and active domain utilities. `vditor-i18n.ts` maps the localized application catalog to Vditor's toolbar contract. Obsolete Vue-era route-name, touch interception, caret/table editor, and `motion-v` compatibility helpers are intentionally removed rather than excluded from checks.
- `src/lib/backend-security.ts` — shared Firebase ID token plus App Check request-header construction for browser-to-Worker API calls.
- `src/lib/request.ts` — shared timeout and cancellation boundary for browser requests; already-aborted parents short-circuit before an operation can start, while active aborts and deadlines reject even if the underlying operation ignores its signal.
- `src/lib/abort-signal.ts` — shared abort-aware async race that rejects pre-aborted work immediately and removes listeners after settlement.

- `src/hooks/use-paged-request-guard.ts` — shared feed request generation/in-flight guard that prevents duplicate loads and stale query responses from committing.
- `src/lib/content-entity-store.ts` and `src/hooks/use-content-entity.ts` — normalized user-scoped content entities shared by lists, details, mutations, and realtime; explicit issue/announcement summary types omit full bodies and cannot satisfy or overwrite authoritative detail reads, while field revisions prevent older requests from replacing newer optimistic, server-confirmed, or realtime patches. A remote content-version mismatch clears the affected domain before its authoritative reload.
- `src/lib/reaction-state.ts` — shared pure optimistic reaction state transition used by proposal support, facility affected, and announcement like flows so immediate counts and rollback snapshots follow one rule.
- `src/lib/view-memory-cache.ts` — bounded 30-minute, user-scoped LRU snapshots for primary list/query UI, pagination and dashboard state; hooks repaint cached views synchronously, refresh through existing services, and clear snapshots with the active session.
- `src/lib/feed-page-limit.ts` — runtime-configured retained-page ceiling for content and notification feeds so long browsing sessions keep bounded React state and DOM size.
- `src/lib/category-management-state.ts` — pure category-draft construction, identity validation, and default-preserving removal used by the management hook.
- `src/lib/platform-job-events.ts` — tiny same-tab refresh signal shared by settings mutations and the observable background-job panel.
- `src/lib/supported-issue-memory.ts` — non-reactive session-scoped support memory used by issue reads and optimistic reactions, keeping support toggles out of the global session render path.
- `src/lib/notification-target.ts` — pure direct notification-to-route resolution used for immediate navigation without a preliminary content fetch.
- `src/lib/interactive-target.ts` — the pure reading of an event into the control and internal link it landed on. Navigation feedback and optimistic detail presentation ask the same question, so they ask it in one place.
- `src/lib/ndjson.ts` — newline-delimited JSON read off a response as it arrives, holding back a line until the newline that completes it.
- `src/lib/realtime-timing.ts` — the two clocks the realtime transport runs on: the inactivity window that bounds an idle WebSocket's lifetime, and the heartbeat interval that keeps a live one from being dropped in silence.
- `src/constants/share.ts` — the mark a shared link carries and the routes that earn it, named where both the share and the handoff out of an in-app browser can reach them. Only a single piece of content counts: someone handed the front door or a feed is meeting Novae for the first time and is still invited to install it.
- `src/constants/admin-activity.ts` — the periods the administration overview reports on and the four figures a period is read as, named once for the overview, the figure's own screen, and the full activity record.
- `src/constants/` — generated/static application, account-access presets, category, status, retention, API error, and rate-limit constants.
- `src/types/` — shared frontend/domain types.
- `src/i18n/` — reactive React i18n store and paired `en` / `zh-TW` domain catalogs. `ui.ts` contains the rebuilt interface language; only explicit user choices persist the locale locally.
- `src/generated/` — generated frontend contracts; do not edit manually. `motion-ladder.css` and `motion-tokens.ts` are the two halves of the same motion ladder, so a stylesheet recipe and a Motion transition cannot drift apart.
- `cloudflare/src/index.ts` — sole public API entrypoint for actions, App Check and Firebase auth ingress, auth sync, realtime tickets/WebSockets, signed media, Cloudinary webhooks, Queue consumption, and scheduled maintenance. The Firebase ID token is verified exactly once here and the resulting user is handed to the action, sync and realtime-ticket paths; a Queue batch collapses into one sweep because every message asks for the same bounded work.
- `cloudflare/src/app-check.ts` / `turnstile.ts` — Worker-side Firebase App Check JWT validation and Cloudflare Turnstile Siteverify boundaries; App Check is required for browser API requests and Turnstile gates first-time profile creation.
- `cloudflare/src/backend/actions/` — generated-registry-driven action dispatch and domain workflows. Authorization is enforced here and in database functions, never by frontend visibility checks. An answer leaves as newline-delimited JSON: `response.ts` writes the `start`/`part`/`end` lines, `execution.ts` turns an action's return value into the pieces they carry, and the first piece is produced before the response exists so an outright failure is still an ordinary error response with its own status.
- `cloudflare/src/backend/actions/segments.ts` — several independent reads sent in the order they come back, which is how an action whose answer is assembled from reads that do not need each other stops waiting for the slowest one.
- `cloudflare/src/backend/actions/category-catalog.ts` — the categories as every other action reads them: the runtime category shapes, the policy lists derived from them, and the catalog a session is bootstrapped with. `categories.ts` keeps the admin side that rewrites them.
- `cloudflare/src/backend/actions/upload-delivery.ts` — who may see an uploaded image and how it reaches them: the access an upload inherits from the issue, comment, announcement or facility it is attached to, resolved for a whole batch in one read per target kind, and the signed delivery URLs that follow from it. `uploads.ts` keeps the upload session's own lifecycle.
- `cloudflare/src/backend/database/` — the PostgreSQL connection, multi-statement transaction support (`transaction(async (tx) => ...)`), the `sql`/`sqlOne`/`sqlMaybe` tagged template every statement is written in (each interpolation becomes a bound parameter), the typed `call()` into `app_api`, and generated table/RPC contracts introspected from the fully migrated PostgreSQL schema. `Row<T>` and `Selected<T, columns>` name what a statement returns, so a column a migration renames fails the build where it is read.
- `cloudflare/src/backend/events/domain-events.ts` — the event vocabulary, the finished write the domains read, and the dispatch to `issue-events.ts`, `facility-events.ts`, `announcement-events.ts` and `platform-events.ts`. An action no domain claims fails here rather than going out silently.
- `cloudflare/src/backend/events/{issue,facility,announcement,platform}-events.ts` — what one write announces, per domain: the event and the channels it is delivered to (`notion`, `in_app`, `push`, `realtime`), recorded atomically within the write transaction.
- `cloudflare/src/backend/shared/notion-*.ts` — the Notion archive in one piece each: `notion-api.ts` is the HTTP boundary, the schema it grows and the pacing that keeps a rebuild from being refused for going too fast, `notion-timeline.ts` writes the entries a page is missing — the page read once, everything missing appended a hundred blocks at a time, so a proposal with a long discussion costs a handful of requests instead of more than a Worker invocation is allowed to make, `notion-page.ts` the page a piece of Novae has and the vocabulary it is written in, `notion-event.ts` the event a delivery hands over, `notion-sync.ts` dispatches content to its domain writer, `notion-audit-events.ts` turns every administrator write into one Chinese system-operation page, and `notion-reconcile.ts` rebuilds current proposals, facilities, announcements, surviving comments, and operations from PostgreSQL a bounded pass at a time — bounded by what the whole Worker invocation has spent of its request allowance rather than by the pages a pass writes — and only writes: pages already in the workspace are the administrator's to remove. The queues a rebuild supersedes are cleared by the `rebuildNotionArchive` action when it is asked for, not when the job runs, so the operations screen stops reporting them immediately.
- `cloudflare/src/backend/jobs/` — event delivery by destination (`notion-deliveries.ts`, `notification-deliveries.ts`, `realtime-deliveries.ts`) over the shared claim-and-settle of `delivery-attempt.ts`, with what a notification says in `notification-content.ts` and who hears it in `delivery-recipients.ts`; background job execution (`background-jobs.ts`) and consumer orchestration (`consumer.ts`) driven by Cloudflare Queues, replacing legacy separate outbox, deletion, and realtime processors. A sweep asks `app_api.pending_delivery_destinations()` once and claims only for the destinations that have work, so an empty drain costs one round trip instead of five.
- `cloudflare/src/backend/shared/` — Worker environment, Firebase token validation, account access rule resolution, Cloudinary, FCM, idempotent Notion sync with timeline markers and pagination, HTTP, media, platform runtime settings, and structured observability with version 2 correlation schema.
- `cloudflare/src/durable/` — SQLite-backed business rate limits and WebSocket Hibernation realtime hub, whose heartbeat is answered by the runtime's own auto-response so a ping never wakes the object. Realtime state can reconnect from PostgreSQL content versions instead of becoming a source of record.
- `cloudflare/src/rate-limit.ts` / `cloudflare/src/media.ts` — opaque per-UID native limiter keys, invalid-auth IP breakers, and viewer-scoped media limiter keys; the Turnstile-protected login ingress permits a 300-request/minute shared-network burst before its IP breaker, while authenticated traffic is limited per UID instead of penalizing valid school traffic behind one public IP.
- `cloudflare/wrangler.json` — local/default Worker bindings for Hyperdrive, Queue, Durable Objects, cron, native rate limits, observability, and Smart Placement; deployment renders an ignored environment-specific copy.
- `database/migrations/` — fresh PostgreSQL 17 baseline and append-only migrations for Neon. `0002` bootstraps roles, `0003` completes realtime batches, `0004` seals the Worker-only database boundary, `0005` exposes scheduled support expiry, `0006` adds runtime settings, `0007` adds admin overview, user interaction restrictions, and audit-log data/functions, `0008` aligns admin user/activity visibility and adds audit-log retention cleanup, `0009` applies the complete retention lifecycle, `0010` adds permanent Notion activity archives, `0011` seeds content versions from migration time, `0012` adds permission-checked failed Cloudinary deletion recovery without exposing or changing Notion data, `0013` adds observable bounded platform policy jobs, `0014` moves the full retention surface into runtime settings plus bounded retention batches, `0015` aligns runtime retention and cleans orphaned Notion mappings, the forward `0015_z` bridge quiesces legacy projection triggers and `0015_zz` creates the no-login runtime role shell before populated or fresh clusters enter `0016`, and `0016` establishes system data consistency with atomic operations, domain events, unified event deliveries, background jobs, aggregate revisions, canonical operation/delivery/job retention keys, non-negative counter invariants, deduplicated Notion mapping, and complete retirement of legacy outbox, idempotency, and deletion tables, `0017` scopes review-queue visibility, `0018` carries per-status category counts on first list pages, and `0019` widens feed search from the title to the body and the author's display name, with the author matched only where the category shows it.
- `database/seed.local.sql` / `seed.integration.sql` — deterministic sample and backend-test seeds; production deploys never run either seed.
- `config/` — source JSON for generated contracts, categories, limits, retention, and the motion ladder.

## Verification and delivery

- `database/migrations/0024_content_safety_ceilings.sql` — database storage ceilings above the admin-configurable Worker and frontend product limits.
- `database/migrations/0025_consumer_fencing.sql` — database-assigned claim tokens prevent late job and delivery consumers from overwriting newer attempts, including manual retries.
- `database/migrations/0026_external_cleanup_backlog.sql` — retains compact identifiers for unresolved external deletions after verbose failed-job logs expire, with administrator retry.
- `database/migrations/0027_extended_runtime_policies.sql` / `cloudflare/src/media-policies.ts` — runtime per-user burst limits, bounded frontend/media cache policies and delivery batch settings; media policy reads are coalesced for one minute.
- `database/migrations/0028_admin_pagination.sql` — bounded administrator user and audit pages; the page controls live inside `src/components/ui/record-list.tsx`.
- `src/lib/admin-routes.ts` — the administration route table with the permission each area needs, holding no JSX so permission-aware navigation can read it without a hook reaching into a component.
- `database/migrations/0029_archive_and_backup_policies.sql` / `database/migrations/0030_remove_github_database_backups.sql` — 0029 introduced Notion archive cleanup and GitHub backup policies; 0030 removes the retired backup policy values and history after moving database recovery to Neon's native restore window.
- `database/migrations/0031_author_support_is_counted.sql` — the author's own support belongs to a proposal's count: the support toggle reads the count the projection trigger writes instead of overwriting it with the bare supporter rows, drifted counts are repaired, and deletion carries the proposal's read access and status out of the transaction so a deleted proposal reaches the audience the live one had.
- `database/migrations/0032_overview_activity_limit.sql` — the overview reads a hundred recent activities rather than fourteen, because each figure for the period now opens onto its own share of them; the drawer says so when a figure still counts more than the reading can show.
- `database/migrations/0033_pending_delivery_probe.sql` — exposes the destinations with claimable event deliveries so a queue sweep asks once what actually needs work before claiming destination batches.
- `database/migrations/0034_realtime_heartbeat_policy.sql` — adds the configurable realtime heartbeat interval to existing stored operation-policy snapshots.
- `database/migrations/0035_rebuild_notion_archive.sql` — queues one complete Notion archive rebuild after the full metadata schema and Chinese system-operation records ship.
- `database/migrations/0036_retire_deletion_job_console.sql` — retires the deletion-only job reader and its retry; the operations console lists and retries every background job, deletion included, so the same failure stopped being two rows saying different things about itself.
- `database/migrations/0037_avatar_cache_allowance.sql` — raises the daily avatar-caching allowance now that a browser asks for it once a day rather than once a visit.
- `database/migrations/0038_forget_rate_limit_refusals.sql` — drops the rate-limit refusals the failure screen used to count, now that the worker no longer records them.
- `database/migrations/0039_account_access_rules.sql` — expands account restrictions into UID or email-local-part prefix rules with read-only, reaction-only, and login-blocked presets while preserving existing restrictions as UID read-only rules.
- `database/migrations/0040_fix_support_goal_deadline.sql` — qualifies the support-goal response deadline parameter so the threshold-crossing update cannot conflict with the identically named issue column.
- `database/migrations/0041_remove_response_deadlines.sql` — removes response-deadline columns, RPC arguments, policy snapshots and JSON fields without changing support deadlines or access rules; replaces affected live functions and revokes public execution on recreated write functions. Historical migrations are preserved for upgrades.
- `database/migrations/0042_platform_admin_notification_preferences.sql` — removes ordinary users' per-kind Push preferences and unregister RPC, makes Push enablement one-way in the application, and adds default-off proposal, facility, and comment preferences that filter platform administrators across both in-app and Push delivery.
- `database/migrations/0043_announcement_notice_state.sql` — adds the independent per-account announcement-list cursor, rollout baseline, new-account initialization, unread probe, and bounded mark-through write used by the header notice.
- `database/migrations/0044_category_author_deletion_policy.sql` — adds default-off proposal and facility author-deletion policy per category, exposes it through bootstrap and management catalogs, and requires either scoped management authority or the matching author policy in the database deletion functions.
- `tests/integration/retired-response-deadlines.test.ts` — checks the migrated database no longer exposes deadline columns/arguments, and recreated write functions remain private with explicit Worker execution grants.
- `tests/e2e/record-surfaces.spec.ts` — read-only browser regressions for notification history, direct/reloaded record sheets, long supporter-list boundaries, header backdrop coverage, global overscroll and consistent row feedback at phone/desktop sizes. `NOVAE_E2E_SURFACE_BROWSER=webkit` (or `firefox`/`chromium`) runs only bootstrap plus this suite through the normal E2E environment; default CI keeps its Chromium projects.

- `database/migrations/0023_policy_batch_state.sql` — durable policy-batch progress, multi-batch lifecycle constraints, retry state and persisted batch failures.

- `database/migrations/0021_event_retention.sql` — bounded domain-event expiry allowing unreferenced operations to expire without breaking idempotent responses.

- `tests/integration/operations-console.test.ts` — verifies administration operational workflows through the production background consumer and real PostgreSQL state.
- `tests/integration/durable-rate-limits.test.ts` — real workerd/SQLite Durable Object tests for concurrent quotas, shared-IP UID isolation, atomic rejection and idle expiry alarms.
- `tests/unit/write-cooldown.test.ts` — verifies bounded frontend write spacing and cancellation without dropping ordinary rapid interactions.
- `tests/integration/provider-diagnostics.test.ts` — provider-contract tests for safe Worker log projection; these are not live provider acceptance tests.

- `database/migrations/0020_operational_job_execution.sql` — separates external background-job claims from transactional policy batches and schedules current retention cleanup.

- `tests/e2e/primary-navigation.spec.ts` — real rapid clicks at mobile and desktop widths, cancelled touch selection, delayed navigation feedback, rendered-route agreement, overflow, and reduced motion.
- `tests/e2e/feed-layout.spec.ts` — cold-feed response gating and actual skeleton/content geometry comparisons at 390px and 1440px, plus dense card height, overflow, labelled search, and single-surface navigation assertions.
- `scripts/check-ui-primitives.mjs` / `css-orphan-selectors.mjs` — reject retired Vue references, `transition-all`, arbitrary elevation, ungated hover, orphaned source CSS class selectors, business imports in UI primitives, direct service imports in pages/components, motion tokens the generated ladder does not define, raw durations or easing curves written into a recipe or a JavaScript transition instead of taken from the ladder, spring transitions, and TypeScript modules over 400 lines (300 lines raises a warning). Generated modules and stylesheets are exempt from the line rule. The PostCSS-backed orphan check ignores keyframe percentages and requires a whole class token in product TS/TSX.
- `scripts/check-i18n.mjs` — validates catalog parity/shape/interpolation, API error references, direct `t()` references, and hard-coded Han text across React/TSX sources.
- `scripts/generate-all.mjs` — lock-protected sequential entry point for all generated contracts and font subsets; generated outputs remain committed and verification checks for drift explicitly.
- `scripts/generate-motion.mjs` — turns `config/motion.config.json` into the CSS custom properties the stylesheet uses and the seconds and cubic-bezier arrays JavaScript animations use. It rejects a ladder whose dismissal rungs are not shorter than the rungs they dismiss, and rejects any name outside the ladder.
- `scripts/verify-generated.mjs` / `run-local-verification.mjs` — the shared local/CI generated-artifact drift gate snapshots every committed contract and font shard around `generate:all`, then fast verification covers one strict TypeScript pass, lint, boundary checks, unit tests, architecture boundaries, and tooling policy tests; full local verification additionally runs the production build, build budget, and Bun dependency audit.
- `scripts/database.mjs` — cross-platform PostgreSQL 17 container lifecycle, checksummed forward migrations, deterministic local seeding, and migration status. Remote operation is migration-only and requires an explicit `DATABASE_URL`.
- `scripts/generate-database-contracts.mjs` — introspects the migrated `app_private` columns and `app_api` signatures into the committed Worker database contract; `--check` rejects schema/type drift.
- `scripts/verify-data-consistency.mjs` — read-only anomaly audit for legacy-model retirement, keys/FKs, counters, revisions, operation/event/delivery/job lifecycles, notifications, RBAC, uploads, Notion, Firebase profile coverage, API/event registry drift, and browser cache namespace; Critical/High findings fail verification.
- `scripts/migration-checksum.mjs` — canonical cross-platform migration hashing and immutable applied-migration validation for every current and future SQL migration.
- `scripts/configure-database-runtime.mjs` — creates or rotates the `novae_runtime` login, grants only DML, sequence use, and function execution, verifies those credentials with a real application-table query, and can hand the verified URL to deployment for immediate Hyperdrive synchronization; it grants no DDL or role-management capability.
- `scripts/configure-cloudinary.mjs` — idempotently provisions the authenticated image upload preset during deployment, with explicit provider errors instead of healthcheck side effects.
- `scripts/reset-cloudinary.mjs` — explicit destructive Cloudinary Admin API cleanup used only by the protected reset workflow.
- `scripts/verify-integration.mjs` / `verify-populated-migration-upgrade.mjs` — single Node.js orchestration for PostgreSQL, Windows WSL runtime lifetime, least-privilege role setup, dynamically allocated provider receiver ports, npm-resolved Firebase Auth Emulator, Bun-launched Next.js, Wrangler, integration/stress tests, and Playwright. The served environment runs one level deeper as a detached session that treats its launcher's exit as the stop signal, because `bun run` kills its own child outright on Ctrl+C and no shutdown handler in that child can finish. Backend verification additionally constructs a real populated pre-0016 database and migrates it through the consistency cutover; served/E2E runs use the local sample seed.
- `scripts/render-worker-config.mjs` — validates the Hyperdrive ID and renders relocatable environment-specific Worker/Queue names, entry paths, native rate-limit namespace IDs, and optional Notion state without committing deployment bindings.
- `scripts/external-provider-test-server.mjs` — isolated Cloudinary, FCM, and Notion-compatible receiver used only by integration verification.
- `scripts/generate-harmonyos-subset.mjs` / `check-build-budget.mjs` — derive the used Traditional Chinese HarmonyOS Sans shards from source text, then enforce configurable Next build asset/font/JS/CSS budgets with near-limit warnings.
- `.gitignore`-excluded `vite.config.js` / `vite.config.d.ts` — legacy local Vite artifacts are outside the current Next.js application and are excluded from ESLint and repository ownership.
- `tests/unit/` — Vitest behavior tests only: domain logic, untrusted Markdown sanitization, request cancellation/timeout, persistent-cache and realtime timing policy, desktop/installed-PWA notification prompt eligibility, which routes a shared link can point at, migration checksum, and database-client concurrency. Tests assert observable behavior, never the text of a source file; structural rules belong in `check:ui` or `tests/architecture/`.
- `tests/architecture/` — stable AST-backed system boundaries only: frontend dependency direction, UI primitive purity, database ownership, frontend/Worker contracts, provider ownership, and observability. Package-management, generated-artifact, command/CI policy, and source hygiene live in `tests/tooling/`; helper algorithm details live in `tests/unit/`.
- `src/components/admin/admin-activity-feed.tsx` — activity rows shared by the overview's period figures and the cursor-paginated full history, which is the second view of `/admin/audit` rather than a dialog. A row names its kind only where the group around it does not already say so.
- `tests/integration/` — backend actions, category-scoped authorization, least-privilege database boundary, RPCs, jobs, retention, Worker ingress/realtime behavior, transaction-boundary fault injection, concurrent operation idempotency, canonical API JSON, Notion pagination/duplicate repair, correlation/revision delivery checks, and a concurrent 100-user profile-sync/session-bootstrap burst; required for backend changes.
- `tests/e2e/` — Playwright bootstrap plus authenticated desktop/mobile workflows, action-response correlation assertions, category/scope combinations, multi-scope revocation isolation, content reactions/comments/results/deletion, account restriction, platform settings, notifications, upload lifecycle coverage, and browser-level route/dropdown/reduced-motion animation verification, including single-page route stacking and single-card detail state replacement.
- `scripts/wsl.mjs`, `scripts/database.mjs`, `scripts/verify-integration.mjs` — automatic single-distro WSL selection (interactive selection when several are installed), root-owned on-demand Docker lifecycle with systemd autostart disabled and a readiness probe that waits for the daemon to accept connections before any container work, non-restarting local PostgreSQL ownership, and failure/Ctrl+C-safe teardown of every local verification service; a distro started solely for verification is terminated afterward to release memory.
- `.github/workflows/verify-and-deploy.yml` — the single verification and delivery gate for pull requests, direct `main`/`dev` pushes, and scoped manual dispatch; Node 24 plus Bun 1.4 run fast checks and affected backend verification alongside two isolated browser-E2E shards, then both backend and Vercel deployment jobs wait for every relevant verification and start together. Browser verification retains its Firebase Emulator / Next compiler cache, while the frontend deploy job keeps the Vercel build output and dependencies on one runner.
- `.github/workflows/reset-database-and-cloudinary.yml` — protected manual disaster-reset flow: after an exact confirmation string, resets the application schemas, reapplies migrations, restores the Worker runtime role, clears Cloudinary resources, and restores the upload preset.

## Repository documentation

- `暫存.md` — user-requested temporary implementation and verification tracker for the administration operations console.

- `README.md` — Traditional Chinese project entry with stack badges, product scope, architecture overview, local quick start, operational boundaries, and links to repository-owned documentation.
- `docs/README.md` — documentation index.
- `docs/product.md` — users, content workflows, initial setup, administration scope, and interface principles.
- `docs/ui-design.md` — website brand mapping, dense application spacing, shared-component ownership, and loading/motion acceptance criteria for the taste-guided redesign.
- `docs/routes-and-permissions.md` — complete application route map, role and permission codes, category scopes, backend authorization order, restricted-user behavior, and issue visibility rules.
- `docs/architecture.md` — browser, Worker, PostgreSQL, Queue, Durable Object, Firebase, Cloudinary, and Notion boundaries plus write/event flow.
- `docs/local-development.md` — prerequisites, the complete emulator environment, manual frontend work, local database commands, and generated artifacts.
- `docs/configuration.md` — frontend, Worker, database, Cloudflare, provider, and Vercel environment-variable reference.
- `docs/backend-and-data.md` — public Worker routes, generated action registry, database permissions, forward migrations, domain events, jobs, retention, and generated database contracts.
- `docs/events-realtime-and-media.md` — domain-event destinations, Queue behavior, realtime topics and cache invalidation, notification/Push flow, and signed Cloudinary upload/delivery lifecycle.
- `docs/runtime-policies.md` — exact business and Cloudflare ingress limits, image-processing constraints, and initial runtime retention settings.
- `docs/deployment-and-operations.md` — branch-to-environment mapping, gated backend/frontend deployment, Neon database recovery, maintenance, and destructive reset behavior.
- `docs/testing.md` — local, integration, browser, stress, and full verification commands plus test-suite ownership.
- `PRODUCT.md` — product purpose, users, features, and explicitly approved runtime migration.
- `AGENTS.md` — the operating contract for any agent editing this repository: absolute rules, where each kind of file belongs, dependency direction, split thresholds, where each kind of test belongs, and the verification commands. Read it before `structure.md`.
