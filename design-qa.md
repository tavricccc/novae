# Design QA

- References: `C:/Users/tavri/Downloads/IMG_3460.PNG`, `IMG_3461.PNG`, `IMG_3462.PNG`, `IMG_3463.PNG`, `IMG_3470.PNG`, and `C:/Users/tavri/AppData/Local/Temp/codex-clipboard-86d23466-a1ce-43c6-8b9a-d7a31e029bca.png`.
- Target: correct mobile sheet padding, natural-height action menu, keyboard-stable route-based creation flows, clearly layered system-settings navigation and category editing, independently presented desktop settings/notification popups with compact balanced padding, aligned Bottom Tab/detail footer spacing, continuous route/header/detail-tab motion, slightly heavier dialog top padding, centered sidebar avatar, and retained neumorphic shadows without iOS WebKit ghost layers.
- Structural review: route pages now overlap during forward/back/crossfade transitions instead of leaving an `out-in` blank frame; the persistent mobile header animates its back-button width and opacity while title content crossfades, and mobile detail content/comments use directional transitions; iOS keeps these shadow-bearing transitions opacity-only. Sheet handle removed from layout flow; padded dialogs add a small top optical adjustment; action menu uses content height; proposal/facility/announcement creation share dedicated full-bleed `/new` route pages tied to Visual Viewport height and unsaved-navigation guards; their desktop footer reserves button-shadow bleed, while mobile uses an iOS-like 1rem side inset and a compact bottom inset that still accounts for the Home Indicator; mobile detail bottom-safe spacing reuses the measured Bottom Tab screen gap, so its time/action footer has the same gap above the Tab as the Tab has below it; system settings and Setup use the same equal-width feature segments and selected-category editor, keep feature switches in the batch-save draft, disable category fields while the draft feature is off, keep only the unique-default switch, and treat deletion as permanent removal of all related records; Setup explicitly confirms that manager assignment is deferred until people register; mobile category selection scrolls horizontally; desktop utility panels use compact balanced outer padding without sheet-handle compensation, while their inner scroll containers reserve shadow bleed so card elevation is not clipped; skeleton opacity runs on a shadow-free inner node; keyed list teardown no longer recreates the shadow tree.
- Automated review: `verify:local` passed; the separately rerun `verify:integration` passed 18 tests with 0 failures.
- Visual comparison: blocked because this repository's `AGENTS.md` explicitly prohibits in-app browser preview. No browser screenshot was captured.

final result: blocked
